interface:
  display_name: "AI Team Supervisor"
  short_description: "Route repo runs across agents and skills"
  default_prompt: "Use $ai-team-supervisor to route the current run and call the next agent skill."
