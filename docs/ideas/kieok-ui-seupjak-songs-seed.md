# Songs Seed For 기억의 습작

Expose the currently extracted lead-sheet draft for `기억의 습작` in the `/songs` library so it can be opened in the existing score viewer.

Constraints:

- Keep the change additive.
- Do not block on a full lead-sheet publish pipeline.
- Preserve visible uncertainty in chart notes instead of pretending the chart is fully verified.
