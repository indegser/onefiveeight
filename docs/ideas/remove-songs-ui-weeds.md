# Remove Songs UI Weeds

Clean up visual clutter on the `/songs` page while preserving the existing song list and detail behavior.

Acceptance intent:

- Keep the previously removed temporary sample out of the songs list.
- Compress the page header so it acts like an internal tool surface, not a marketing intro.
- Reduce row-level clutter by removing always-visible long summaries and consolidating metadata.
- Preserve accessible link rows and existing empty state behavior.
- Verify the page before and after edits in a browser, including representative rows and available-width fit.
