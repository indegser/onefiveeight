# Goodbye MP3 Audio Publish

Use the user-provided MP3 for `Goodbye` to validate the existing practical chart and publish a draft lead sheet that can live alongside the `/songs` library entry.

Constraints:

- Route the work through the supervisor run workflow and keep artifacts in sync.
- Reuse the existing `Goodbye` chart structure instead of inventing a new one from raw MIR bars.
- Preserve explicit uncertainty where the audio analysis remains probabilistic.
- Keep the current songs workspace behavior unchanged.
