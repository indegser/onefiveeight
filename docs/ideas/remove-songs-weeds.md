# Remove Songs Page Weeds

Clean up the `/songs` page by removing the temporary renderer sample entry that is visible to users as a copied chart.

Acceptance intent:

- The songs index should no longer list the `잔향` photographed chart renderer sample.
- Direct lookup for that sample should no longer be generated as a public song detail route.
- Existing real copied charts and the lead-sheet renderer should keep working.
- Verify the page before and after the change in a browser.
